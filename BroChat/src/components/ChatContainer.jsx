import React from 'react'

const ChatContainer = (props) => {
  return (
    <div className={`w-1/2 bg-gray-200 p-4 ${props.selecteduser? "rounded-r-lg" : "rounded-none"}  `}>
      cc
    </div>
  )
}

export default ChatContainer
